#pragma once
#include <vector>
#include <string>

std::vector<std::string> unpackList(std::string input);
std::vector<float> unpackFloatList(std::string input);
std::vector<bool> unpackBoolList(std::string input);
